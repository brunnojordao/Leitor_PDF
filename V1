from PyPDF2 import PdfReader

reader = PdfReader("balancete exemplo.pdf")
number_of_pages = str(len(reader.pages))
page = reader.pages[0]
text = page.extract_text()


arquivo = open('arquivo.txt','w')
arquivo.write(text)
